node_modules/
dist/
.env
/input
/output
# Local working artifacts, not part of the repo: Claude state/skills,
# planning notes, and generated docs/reports.
.claude
plans
docs/*
# Exceptions: agent config and ADRs are repo-durable — CLAUDE.md points at
# them, so they must survive a fresh clone.
!docs/agents/
!docs/adr/
